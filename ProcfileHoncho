Web: pip install -r fastapi/requirements.txt && uvicorn fastapi/Gender/main:app --host 0.0.0.0 --port $PORT && pip install -r streamlit/requirements.txt && streamlit run streamlit/app.py --server.port $PORT